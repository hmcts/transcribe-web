[supervisord]
nodaemon=true
pidfile=/tmp/supervisord.pid
logfile=/dev/null
logfile_maxbytes=0

[program:caddy]
command=caddy run --config /etc/caddy/Caddyfile --adapter caddyfile
autostart=true
autorestart=false
environment=HOME="/tmp"
stdout_logfile=/dev/stdout
stdout_logfile_maxbytes=0
stderr_logfile=/dev/stderr
stderr_logfile_maxbytes=0

[program:nextjs]
command=node /app/start.js
autostart=true
autorestart=false
environment=PORT="3001",HOSTNAME="127.0.0.1"
stdout_logfile=/dev/stdout
stdout_logfile_maxbytes=0
stderr_logfile=/dev/stderr
stderr_logfile_maxbytes=0

; Kill supervisord (and therefore all other programs) when any program exits.
; supervisord's graceful shutdown then sends SIGTERM to each remaining process.
[eventlistener:quit_on_exit]
command=/bin/sh -c "printf 'READY\n'; IFS= read -r _; kill -TERM $PPID; printf 'RESULT 2\nOK'"
events=PROCESS_STATE_EXITED,PROCESS_STATE_FATAL
stdout_logfile=/dev/null
stderr_logfile=/dev/stderr
stderr_logfile_maxbytes=0
